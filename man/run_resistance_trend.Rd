% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ir_trend_plots.R
\name{run_resistance_trend}
\alias{run_resistance_trend}
\title{Generate insecticide resistance trend scenarios}
\usage{
run_resistance_trend(
  df_IR,
  year_cut = 2025,
  n_years_to_add = 5,
  continue_trend_start_year = 2010,
  model_type = "unchanged",
  trendmethod = c("constantTrend", "continuedTrend", "hybridTrend"),
  group_cols = c("adm0", "adm1"),
  value_col = "mean_ir"
)
}
\arguments{
\item{df_IR}{Data frame containing insecticide resistance data with
grouping columns, year column, and value column.}

\item{year_cut}{Numeric. Last year of observed data. Projections start
from \code{year_cut + 1}. Default is \code{2025}.}

\item{n_years_to_add}{Numeric. Number of years to project forward.
Default is \code{5}.}

\item{continue_trend_start_year}{Numeric. First year to include in model
fitting for continued trend. Default is \code{2010}.}

\item{model_type}{Character scalar. Regression family for continued trend.
One of \code{"gaussian"}, \code{"binomial"}, \code{"quasibinomial"}, \code{"lm"}, \code{"gam"},
or \code{"unchanged"}. If \code{"unchanged"}, uses existing data values directly
without refitting. Default is \code{"unchanged"}.}

\item{trendmethod}{Character scalar. Projection method: \code{"constantTrend"}
(flat-line from last observed value), \code{"continuedTrend"} (model
extrapolation), or \code{"hybridTrend"} (average of constant and continued).
Default is \code{"constantTrend"}.}

\item{group_cols}{Character vector. Names of grouping columns (e.g.,
administrative units, insecticides). Default is \code{c("adm0", "adm1")}.}

\item{value_col}{Character scalar. Name of the mortality/resistance value
column. Default is \code{"mean_ir"}.}
}
\value{
A data frame with grouping columns, \code{year}, \code{trendmethod}, and the value
column specified in \code{value_col}.
}
\description{
Extends mortality time series beyond a specified year using one of three
projection methods: constant trend (flat-line), continued trend (model
extrapolation), or hybrid trend (average of both).

Supports flexible grouping and can handle any IR metric column name.
}
\examples{
\dontrun{
ir_data <- data.frame(
  adm0 = "BDI",
  adm1 = rep(c("Province A", "Province B"), each = 16),
  year = rep(2010:2025, 2),
  mean_ir = runif(32, 0.6, 0.95)
)

constant_proj <- run_resistance_trend(
  df_IR = ir_data,
  year_cut = 2025,
  n_years_to_add = 5,
  trendmethod = "constantTrend"
)

hybrid_proj <- run_resistance_trend(
  df_IR = ir_data,
  year_cut = 2025,
  trendmethod = "hybridTrend"
)
}

}
