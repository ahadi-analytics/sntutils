% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_boundary_artefacts.R
\name{clean_boundary_artefacts}
\alias{clean_boundary_artefacts}
\title{Clean shapefile boundary artefacts and slivers}
\usage{
clean_boundary_artefacts(
  shp_path,
  metric_crs = 32736,
  sliver_threshold_km2 = 0.01,
  snap_precision = 1e+05,
  group_col = NULL
)
}
\arguments{
\item{shp_path}{Character. Path to the input shapefile or geojson.}

\item{metric_crs}{Integer. EPSG code for metric CRS used for area
calculations. Default 32736 (UTM 36S, East Africa).}

\item{sliver_threshold_km2}{Numeric. Area threshold in km² below which
polygons are considered slivers. Default 0.01.}

\item{snap_precision}{Numeric. Precision value passed to
\code{sf::st_set_precision()}. Default 1e5 (~1m grid).}

\item{group_col}{Character or NULL. Column name to group by when rebuilding
topology (nuclear option). If NULL, rebuild step is skipped.}
}
\value{
A named list with:
\item{shp_clean}{Cleaned sf object in original CRS}
\item{diagnostics}{List of diagnostic counts pre-fix}
\item{slivers_removed}{Integer count of slivers removed}
}
\description{
Diagnoses and fixes visual artefacts (sliver polygons, choppy black lines)
in shapefiles by applying a sequential set of repairs: precision snapping,
zero-buffer topology fix, sliver removal, and optionally a full topology
rebuild. Returns a cleaned sf object and a diagnostic summary list.
}
\examples{
\dontrun{
result <- clean_boundary_artefacts(
  shp_path = here::here("data", "raw", "districts.geojson"),
  metric_crs = 32736,
  sliver_threshold_km2 = 0.01,
  snap_precision = 1e5,
  group_col = NULL
)
plot(sf::st_geometry(result$shp_clean))
}
}
