% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_emod_demog.R
\name{write_emod_demog_by_adm2}
\alias{write_emod_demog_by_adm2}
\title{Write EMOD Demographics JSON Files by ADM2}
\usage{
write_emod_demog_by_adm2(
  df,
  output_dir,
  folder_case = "title",
  demog_prefix = "",
  demog_suffix = "",
  id_reference = "Legacy",
  individual_properties = NULL,
  age_distribution = NULL
)
}
\arguments{
\item{df}{Data frame with columns: \code{adm2}, \code{lat}, \code{lon}, \code{pop},
\code{crude_death_rate}, \code{crude_birth_rate}.}

\item{output_dir}{Character. Root directory for output folders.}

\item{folder_case}{Case style for subfolder names: \code{"title"}
(default), \code{"upper"}, or \code{"lower"}. Must match the case used
by \code{\link[=write_emod_weather_by_adm2]{write_emod_weather_by_adm2()}}.}

\item{demog_prefix}{Character. Prefix before "demographics" in
the filename. Default \code{""} gives \code{"demographics.json"}. Set
to NULL to auto-prefix with the folder name.}

\item{demog_suffix}{Character. Optional suffix before .json
(e.g. "_wSMC_risk").}

\item{id_reference}{Character. IdReference string. Default
"Legacy". Must match the climate files.}

\item{individual_properties}{Optional list of IP definitions.}

\item{age_distribution}{Optional age distribution list.}
}
\value{
Invisible NULL. Called for side effect of writing files.
}
\description{
Writes one demographics JSON per admin-2 unit, mirroring the
folder layout used by \code{\link[=write_emod_weather_by_adm2]{write_emod_weather_by_adm2()}}. Each
folder gets \code{demographics.json} with matching \code{IdReference},
\code{NodeID = 1}, and \code{NodeCount = 1}.
}
\examples{
\dontrun{
df <- data.frame(
  adm2 = c("Bubanza", "Cibitoke"),
  lat = c(-3.08, -2.89),
  lon = c(29.37, 29.18),
  pop = c(100000L, 120000L),
  crude_death_rate = c(8.5, 8.5),
  crude_birth_rate = c(38.0, 38.0)
)
write_emod_demog_by_adm2(df, tempdir())
}
}
