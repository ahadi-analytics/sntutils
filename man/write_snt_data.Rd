% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_snt_data.R
\name{write_snt_data}
\alias{write_snt_data}
\title{Write an object to standardized filenames in one or more formats}
\usage{
write_snt_data(
  obj,
  path,
  data_name,
  file_formats = "rds",
  date_format = "\%Y-\%m-\%d",
  include_date = TRUE,
  version_tag = NULL,
  n_saved = 3,
  quiet = TRUE,
  overwrite = TRUE,
  ...
)
}
\arguments{
\item{obj}{Object to write}

\item{path}{Directory to write into}

\item{data_name}{Base file name without extension}

\item{file_formats}{Character vector of formats. Default "rds".}

\item{date_format}{Date format for version tag. Default "\%Y-\%m-\%d".}

\item{include_date}{Logical; append _v\if{html}{\out{<date>}}. Default TRUE.}

\item{version_tag}{Optional explicit tag (conflicts with include_date)}

\item{n_saved}{Optional positive integer; keep only the newest
\code{n_saved} versioned files per format. Default is 3.}

\item{quiet}{Logical; suppress info logs.}

\item{overwrite}{Logical; allow overwrite when not versioned.}

\item{...}{Additional writer-specific arguments}
}
\value{
tibble with columns format, path, ok, bytes, message
}
\description{
Writes an object (data.frame, list of data.frames, sf, etc.) to a directory
using standardized naming. Supports atomic writes, optional pruning of older
versioned files, and returns a structured summary.

When filenames are not versioned (no date and no tag) and a file already
exists, set \code{overwrite = TRUE} to replace it. Otherwise an error is thrown.
}
\examples{
# write_snt_data(head(mtcars), tempdir(), "cars", "rds",
#                include_date = FALSE, overwrite = TRUE)
}
