% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snt_data_dict.R
\name{snt_data_dict}
\alias{snt_data_dict}
\title{Load and flatten the SNT variable tree}
\usage{
snt_data_dict(flatten = TRUE, include_schema = TRUE, domain = NULL)
}
\arguments{
\item{flatten}{Logical; if TRUE, returns a tidy tibble of variable metadata.}

\item{include_schema}{Logical; if TRUE, also loads the schema and attaches it
as an attribute.}

\item{domain}{Optional character string; if provided, returns only variables
belonging to that top-level domain (e.g. \code{"routine_data"}, \code{"anemia"}).}
}
\value{
A tibble with columns:
\itemize{
\item \code{domain}: the top-level domain (e.g. metadata, routine_data)
\item \code{snt_var_name}: standardized variable name
\item \code{label_en}, \code{label_fr}, \code{label_pt}: multilingual labels
\item \code{disagg}: character vector of applicable disaggregations (if available)
}
}
\description{
Reads the bilingual indicator hierarchy from \code{inst/extdata/var_tree.yml}
and returns a tidy table of all variables, their domains, labels, and
disaggregation structure (if defined). You can optionally specify a
single domain (e.g. \code{"routine_data"}, \code{"stock_management"}) to filter
the returned results.
}
\examples{

\dontrun{
# Load all variable metadata
sntutils::snt_data_dict()

# Load only routine_data variables
sntutils::snt_data_dict(domain = "routine_data")
}

}
