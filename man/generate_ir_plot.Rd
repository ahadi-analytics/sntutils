% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ir_trend_plots.R
\name{generate_ir_plot}
\alias{generate_ir_plot}
\title{Generate insecticide resistance trend plot}
\usage{
generate_ir_plot(
  data,
  value_col = "mean_ir",
  facet_col = "adm1",
  ncol = 4,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  y_label = "Mortalité (mean)\\n",
  legend_title = NULL,
  continue_trend_start_year = 2010,
  year_cut = 2025,
  width = 16,
  height = 9,
  output_file = NULL,
  dpi = 300,
  compress_image = TRUE,
  image_overwrite = TRUE,
  compression_speed = 1,
  compression_verbose = TRUE,
  show_plot = TRUE
)
}
\arguments{
\item{data}{Data frame containing pre-filtered IR data with year, value
column, facet column, and trendmethod column.}

\item{value_col}{Character scalar. Name of y-axis value column.
Default is \code{"mean_ir"}.}

\item{facet_col}{Character scalar. Name of facetting column.
Default is \code{"adm1"}.}

\item{ncol}{Numeric. Number of facet columns. Default is \code{4}.}

\item{title}{Character scalar. Plot title. If \code{NULL}, no title is shown.}

\item{subtitle}{Character scalar. Plot subtitle. If \code{NULL}, no subtitle
is shown.}

\item{caption}{Character scalar. Plot caption. If \code{NULL}, no caption is
shown.}

\item{y_label}{Character scalar. Y-axis label.
Default is \code{"Mortalité (mean)\\n"}.}

\item{legend_title}{Character scalar. Legend title for trend method.
If \code{NULL}, a default title is generated.}

\item{continue_trend_start_year}{Numeric. First year to show in plot.
Default is \code{2010}.}

\item{year_cut}{Numeric. Year to show vertical reference line indicating
projection start. Default is \code{2025}.}

\item{width}{Numeric. Plot width in inches. Default is \code{16}.}

\item{height}{Numeric. Plot height in inches. Default is \code{9}.}

\item{output_file}{Optional character scalar. File path where the plot
should be saved. If \code{NULL}, the plot is not saved to disk.}

\item{dpi}{Numeric. Resolution (dots per inch) for saved output.
Default is \code{300}.}

\item{compress_image}{Logical. Compress PNG using \code{compress_png()} after
saving. Default is \code{TRUE}.}

\item{image_overwrite}{Logical. Overwrite existing PNG during compression.
Default is \code{TRUE}.}

\item{compression_speed}{Numeric. Compression speed from \code{1} (best quality,
slowest) to \code{10} (fastest, lower quality). Default is \code{1}.}

\item{compression_verbose}{Logical. Print compression statistics.
Default is \code{TRUE}.}

\item{show_plot}{Logical. Display the plot. If \code{FALSE}, returns plot object
invisibly. Default is \code{TRUE}.}
}
\value{
A \code{ggplot} object. If \code{output_file} is provided, the plot is also written
to disk using \code{ggsave()} and optionally compressed.
}
\description{
Creates a faceted time-series plot showing insecticide resistance or
mortality trends with multiple projection scenarios.

Inspired by \code{facetted_map_gradient()} from sntutils. Supports flexible
value columns, faceting, and automatic PNG compression.
}
\examples{
\dontrun{
ir_trends <- data.frame(
  adm0 = "BDI",
  adm1 = rep(c("Province A", "Province B"), each = 48),
  year = rep(2010:2030, 4),
  trendmethod = rep(c("constantTrend", "hybridTrend"), each = 42),
  mean_ir = runif(96, 0.6, 0.95)
)

plot_obj <- generate_ir_plot(
  data = ir_trends,
  value_col = "mean_ir",
  facet_col = "adm1",
  title = "Deltamethrin Mortality Trends",
  output_file = here::here("outputs", "ir_trends.png")
)
}

}
