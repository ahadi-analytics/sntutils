% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_chirps_rasters.R
\name{download_chirps2.0}
\alias{download_chirps2.0}
\title{Download CHIRPS Raster Data from UCSB Archive}
\usage{
download_chirps2.0(dataset, start, end = NULL, out_dir = ".", unzip = TRUE)
}
\arguments{
\item{dataset}{Character. One of the dataset codes listed in
\code{\link[=chirps_options]{chirps_options()}}.}

\item{start}{Character. Start date in \code{"YYYY-MM"} format (e.g., \code{"2020-01"}).}

\item{end}{Character. End date in \code{"YYYY-MM"} format. If \code{NULL}, only
\code{start} month is downloaded.}

\item{out_dir}{Directory path where downloaded files will be saved. Will be
created if it does not exist.}

\item{unzip}{Logical. If \code{TRUE}, the \code{.tif.gz} files will be unzipped after
download.}
}
\value{
No return value. Side effects include downloading CHIRPS \code{.tif.gz}
and \code{.tif} files to the specified folder.
}
\description{
Downloads \code{.tif.gz} CHIRPS rainfall data files from the UCSB Climate Hazards
Group archive for a specified dataset and date range. Supports monthly,
dekadal (3 per month), and pentadal (6 per month) resolutions. Files are
downloaded and optionally unzipped to a local directory.
}
\details{
Use \code{\link[=chirps_options]{chirps_options()}} to view all available datasets and their metadata.
}
\examples{
# View available datasets
chirps_options()

# Download Africa monthly CHIRPS for Jan–Mar 2022
\dontrun{
download_chirps2.0(
  dataset = "africa_monthly",
  start = "2022-01",
  end = "2022-03",
  out_dir = "chirps_data"
)
}

}
