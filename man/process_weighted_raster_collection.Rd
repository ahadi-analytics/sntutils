% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_weighted_raster_collection.R
\name{process_weighted_raster_collection}
\alias{process_weighted_raster_collection}
\title{Process Weighted Raster Data in Batch}
\usage{
process_weighted_raster_collection(
  value_raster_dir,
  pop_raster_dir,
  shapefile,
  id_cols = c("adm0", "adm1", "adm2"),
  value_pattern = "\\\\.tiff$",
  pop_pattern = "\\\\.tif$",
  weight_na_as_zero = TRUE
)
}
\arguments{
\item{value_raster_dir}{Character. Directory containing value raster files}

\item{pop_raster_dir}{Character. Directory containing population raster files}

\item{shapefile}{sf/SpatVector object. Shapefile for spatial aggregation}

\item{id_cols}{Character vector. Column names for spatial identifiers
(default: c("adm0", "adm1", "adm2"))}

\item{value_pattern}{Character. Regex pattern to match value raster files
(default: "\\.tiff$")}

\item{pop_pattern}{Character. Regex pattern to match population raster files
(default: "\\.tif$")}

\item{weight_na_as_zero}{Logical. Treat NA weights as 0 (default: TRUE)}
}
\value{
A data frame containing:
\itemize{
\item Spatial identifiers from id_cols
\item Weighted mean values
\item Metadata (filename, year, month, semester, quarter, date)
}
}
\description{
Processes multiple raster files with population weights, matching value
rasters with corresponding population rasters by year.
}
\examples{
\dontrun{
results <- process_weighted_raster_collection(
  value_raster_dir = "path/to/values",
  pop_raster_dir = "path/to/population",
  shapefile = my_shapefile,
  id_cols = c("adm0", "adm1")
)
}
}
